---
alwaysApply: true
description: Search existing homes before creating new repository assets or docs
---

# Check Before Create

## Rules

- Before creating a new rule, doc, or asset, search `platforms/`, `docs/`, root `.cursor/`, and the selected repo-local assets under root `.codex/` for an existing home.
- Prefer extending an existing asset over creating a near-duplicate.
- Placement rule:
  - repo-maintenance content belongs at the root entrypoints
  - external tool content belongs under `platforms/<tool>/`
- Do not create a second root entry file when an existing one can be clarified or replaced.
